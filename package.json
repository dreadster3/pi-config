{
  "name": "@dreadster3/pi-config",
  "type": "module",
  "version": "1.2.1",
  "description": "Pi coding agent configuration — skills, extensions, prompts, and themes",
  "keywords": [
    "pi-package"
  ],
  "scripts": {
    "postinstall": "for dir in extensions/*/; do [ -f \"$dir/package.json\" ] && npm install --prefix \"$dir\"; done",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.1",
    "@earendil-works/pi-ai": "^0.79.1",
    "@earendil-works/pi-tui": "^0.79.1"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ],
    "skills": [
      "./skills"
    ],
    "prompts": [
      "./prompts"
    ],
    "themes": [
      "./themes"
    ]
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
